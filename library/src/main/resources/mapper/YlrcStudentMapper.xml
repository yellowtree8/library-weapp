<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.library.mapper.YlrcStudentMapper">
    <select id="stuid" resultType="com.example.library.entity.YlrcStudent">
        SELECT	id FROM ylrc_student WHERE user_id = #{user_id}
    </select>
</mapper>
